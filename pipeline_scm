def KUBECONFIG="--kubeconfig=/data_bck/kubeconfig/config"
pipeline {
  agent { label 'common' }
  stages {
    stage('Deploy') {
        steps {
            timeout(time: 1, unit: 'MINUTES') {sh "kubectl apply -f stream.yaml"}
        }
    }    
  }  
}
